import OrbitalAuction from 0xe03daebed8ca0615

pub fun main() {
    var dist = OrbitalAuction.Distribution(UInt64(15050))
    log(dist)

    // var i = 0

    // while i < 500 {
    //     var dist = OrbitalAuction.Distribution(UInt64(i))
    //     log(dist)

    //     i = i + 1
    // }
}